Deliberation transcripts are saved to `deliberations/` by the workflow run, but the workflow doesn't commit/push them back to the repo, so they're lost when the runner exits.

**Task:** In `.github/workflows/senate-review.yml`, after the merge gate runs, commit `deliberations/pr-N.md` back to `main` (bot commit with `[skip ci]`), or attach it as a workflow artifact AND post a summary. Consider race conditions with concurrent deliberations.

**Acceptance:** after a PR is reviewed, its transcript is durably visible in the repo.
