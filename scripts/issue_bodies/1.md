The Senate currently reviews the PR diff as a single truncated blob (16k chars). Large PRs lose context and senators can't see the full picture.

**Task:** Add smart diff chunking in `senate/senators/__init__.py::render_proposal`:
- Split the diff per-file
- Prioritize source files over docs/config (mirror Gittensor's own weighting)
- If the diff still exceeds the budget, summarize the overflow files (path + additions/deletions count) instead of silently truncating

**Acceptance:** unit tests in `evals/` covering a multi-file diff exceeding the limit; senators see every file's existence even when full text doesn't fit.
