A senator that always approves (sycophancy) violates Article II §3 and degrades the whole body. We should measure it.

**Task:** Add per-senator statistics computed from `deliberations/` transcripts: approve rate, agreement-with-majority rate, vote-flip rate after rebuttals, malformed-ballot rate. Output as a markdown dashboard (`docs/SENATE_RECORD.md`) regenerated by a scheduled workflow.

**Acceptance:** dashboard renders from existing transcript format; tests for the parser.
