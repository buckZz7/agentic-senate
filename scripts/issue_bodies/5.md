`senate/llm.py` uses urllib with no retry. A transient 429/500 from the provider crashes the whole deliberation and the verdict falls to ESCALATE unnecessarily.

**Task:** Add bounded retries with exponential backoff (respect Retry-After), distinguish retryable (429, 5xx, timeouts) from fatal (401, 400) errors, and surface per-call latency in the transcript footer.

**Acceptance:** unit tests with a mocked HTTP layer covering retry, backoff cap, and fatal-error passthrough.
