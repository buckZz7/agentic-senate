Senators currently see only the diff. They can't run anything, so subtle breakage is invisible (Article II §4 — Quality demands verification evidence).

**Task:** Run the repo's test suite against the PR head in an *isolated* job (never with secrets — see the security note in senate-review.yml), capture the results, and inject a `TEST EVIDENCE` section into the proposal text senators receive.

**Acceptance:** senators' prompts include real pass/fail evidence; secrets remain unavailable to PR-head code. Document the security reasoning in the PR.
