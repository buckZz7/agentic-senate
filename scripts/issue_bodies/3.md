Article II §5 (Diversity) and Article III §2 (no more than ⌈N/3⌉ senators from one model family) are stated in the Constitution but not enforced in code. `senate/config.yaml` could be edited to violate them.

**Task:** Add a config validator (e.g. `senate/config_check.py`) that enforces:
- odd senator count, 3 ≤ N ≤ 9
- family cap ⌈N/3⌉
- quorum ≤ N

Run it in the tests workflow so an invalid config fails CI.

**Acceptance:** tests demonstrating each violation is caught.
